diff --git a/crates/project/src/lsp_store.rs b/crates/project/src/lsp_store.rs
--- a/crates/project/src/lsp_store.rs
+++ b/crates/project/src/lsp_store.rs
@@ -479,7 +479,7 @@ impl LspStore {
             settings,
             toolchain.clone(),
             delegate.clone(),
-            true,
+            false,
             wait_until_worktree_trust,
             cx,
         );
diff --git a/crates/zed/src/main.rs b/crates/zed/src/main.rs
--- a/crates/zed/src/main.rs
+++ b/crates/zed/src/main.rs
@@ -537,7 +537,7 @@ fn init_ui(cx: &mut App) {
             let options = NodeBinaryOptions {
                 allow_path_lookup: !settings.ignore_system_version,
                 // TODO: Expose this setting
-                allow_binary_download: true,
+                allow_binary_download: false,
                 use_paths: settings.path.as_ref().map(|node_path| {
                     let node_path = PathBuf::from(shellexpand::tilde(node_path).as_ref());
                     let npm_path = settings
diff --git a/crates/remote_server/src/server.rs b/crates/remote_server/src/server.rs
--- a/crates/remote_server/src/server.rs
+++ b/crates/remote_server/src/server.rs
@@ -1249,7 +1249,7 @@ fn init_language_registry(
             let options = NodeBinaryOptions {
                 allow_path_lookup: !new_node_settings.ignore_system_version,
                 // TODO: Implement this setting
-                allow_binary_download: true,
+                allow_binary_download: false,
                 use_paths: new_node_settings.path.as_ref().map(|node_path| {
                     let node_path = PathBuf::from(shellexpand::tilde(node_path).as_ref());
                     let npm_path = new_node_settings
diff --git a/crates/edit_prediction_cli/src/headless.rs b/crates/edit_prediction_cli/src/headless.rs
--- a/crates/edit_prediction_cli/src/headless.rs
+++ b/crates/edit_prediction_cli/src/headless.rs
@@ -84,7 +84,7 @@ pub fn init_headless(cx: &mut App) {
         let settings = &ProjectSettings::get_global(cx).node;
         let options = NodeBinaryOptions {
             allow_path_lookup: !settings.ignore_system_version,
-            allow_binary_download: true,
+            allow_binary_download: false,
             use_paths: settings.path.as_ref().map(|node_path| {
                 let node_path = PathBuf::from(shellexpand::tilde(node_path).as_ref());
                 let npm_path = settings
diff --git a/crates/eval_cli/src/headless.rs b/crates/eval_cli/src/headless.rs
--- a/crates/eval_cli/src/headless.rs
+++ b/crates/eval_cli/src/headless.rs
@@ -86,7 +86,7 @@ pub fn init_headless(cx: &mut App) {
         let settings = &ProjectSettings::get_global(cx).node;
         let options = NodeBinaryOptions {
             allow_path_lookup: !settings.ignore_system_version,
-            allow_binary_download: true,
+            allow_binary_download: false,
             use_paths: settings.path.as_ref().map(|node_path| {
                 let node_path = PathBuf::from(shellexpand::tilde(node_path).as_ref());
                 let npm_path = settings
diff --git a/crates/extension_host/src/extension_host.rs b/crates/extension_host/src/extension_host.rs
--- a/crates/extension_host/src/extension_host.rs
+++ b/crates/extension_host/src/extension_host.rs
@@ -391,9 +391,6 @@ impl ExtensionHost {
-        cx.spawn(async move |this, cx| {
+        cx.spawn(async move |_this, _cx| {
             if let Some(future) = reload_future {
                 future.await;
             }
-            this.update(cx, |this, cx| this.auto_install_extensions(cx))
-                .ok();
-            this.update(cx, |this, cx| this.check_for_updates(cx)).ok();
         })
         .detach();
